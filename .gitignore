.vs/
bin/
obj/
*.user
*.suo
*.vsidx

# Logs
Logs/
*.log

# Local / sensitive settings (never commit real secrets)
appsettings.Local.json
appsettings.*.Local.json
secrets.json

# Runtime user uploads (profile photos, cheque images). The app creates this
# folder at startup; its contents are user data, not source.
KasraLoan.API/wwwroot/uploads/